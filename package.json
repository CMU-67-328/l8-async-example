{
  "name": "Demo",
  "version": "1.0.0",
  "description": "demo project.",
  "scripts": {
    "lite": "lite-server --baseDir ./src",
    "start": "npm run lite"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
